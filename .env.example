# SQLite path (relative to project root or absolute)
DATABASE_PATH=data/clinic.db

# Cookie Secure flag:
# - false|0 = never Secure (local HTTP)
# - true|1 = always Secure
# - unset = Secure when x-forwarded-proto=https or NODE_ENV=production
COOKIE_SECURE=false
